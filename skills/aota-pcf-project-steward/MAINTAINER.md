# Maintainer notes — aota-pcf-project-steward (outside normal LLM runtime)

Preserved from pre-W1 full doc; not loaded via `skill.open` normal path.

- W0 provenance: migrated from `aota-hermes-tools/skills/aota-pcf-project-steward/SKILL.md`; `AOTA_SKILL_CANONICAL_SOURCE=aota_forge`.
- Legacy advisory/docs-continuity semantics (`intake`, `context_prepare`, `docs_update`, `artifact_link`, `close`, `relationship_resolve`, `aota_workspace_list`, `aota_active_task_artifact_open`) superseded by M1/M2 Mode A/B contract. Retained here for archaeology only.
- Deployment/runtime guidance (profile assembly manifest, Hermes loader paths, hash parity vs runtime evidence, managed deploy/recreate, `/aota-runtime` boundary) is operator/maintainer knowledge, not normal steward runtime.
- Document ownership history (README/CHANGELOG vs ROADMAP, reviewer reporting) preserved for governance audits.
- `MAINTAINER_CONTENT_IN_NORMAL_LLM_CONTEXT=no` — this file is never returned by `skill.open`.
