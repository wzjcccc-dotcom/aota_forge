# Maintainer notes — aota-evidence-first-debugging (outside normal runtime)

- W0 provenance: legacy debugger profile contract (`spec_kind=diagnosis`, `mutation_allowed=false`, CodeGraph fallbacks, `aota_debugger_report_submit`, `aota_worker_outcome_submit`, `aota_active_task_artifact_open` preflight) preserved for archaeology.
- Role contract fields (`observed_symptoms`, `reproduction_context`, `suspected_components`, `diagnostic_questions`, `initial_hypotheses`, `evidence_plan`, `mutation_policy`, `confidence_expectation`) are SPEC authoring history.
- `MAINTAINER_CONTENT_IN_NORMAL_LLM_CONTEXT=no`.
